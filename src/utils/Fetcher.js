/* import { ChuckNorrisData } from '../types/ChuckNorris'
import { StarWarsPerson } from '../types/StarWars'

const CHUCK_API_URL = 'https://api.chucknorris.io/jokes/random'
const SWAPI_API_URL = 'http://swapi.dev/api/people' */

/*
	Här samlas alla fetch-requests
*/

class Fetcher {





/* 	static getChuckNorris = async () => {
		const response = await fetch(CHUCK_API_URL);
		const responseJSON = await response.json();
		return responseJSON as ChuckNorrisData;
	};

	static getStarWars = async () => {
		const response = await fetch(SWAPI_API_URL);
		const responseJSON = await response.json();
		return responseJSON.results as StarWarsPerson[];
	}; */

};

export default Fetcher
